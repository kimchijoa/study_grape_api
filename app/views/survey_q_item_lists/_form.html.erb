<%= form_with(model: survey_q_item_list, local: true) do |form| %>
  <% if survey_q_item_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey_q_item_list.errors.count, "error") %> prohibited this survey_q_item_list from being saved:</h2>

      <ul>
      <% survey_q_item_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :survey_q_item_id %>
    <%= form.text_field :survey_q_item_id %>
  </div>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_field :item %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
